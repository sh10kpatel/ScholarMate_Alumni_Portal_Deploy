# Server Configuration
PORT=4000

# Database Configuration (Railway will provide these automatically)
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=
DB_NAME=alumni_db

# Optional: Set to 1 to require database (no in-memory fallback)
# NO_INMEM_FALLBACK=0

# Frontend URL (for CORS if needed)
# FRONTEND_URL=https://your-app.railway.app
